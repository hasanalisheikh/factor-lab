# FactorLab Codebase Audit

## Status

This ledger tracks stabilization findings and their disposition. Secret values are intentionally not
recorded here; environment checks should confirm key presence only.

## Structural findings

| Area              | Finding                                                                     | Status   | Evidence / next action                                                                                                                                                                                                                                                                                                                            |
| ----------------- | --------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Agent policy      | `CLAUDE.md` duplicated shared policy while `AGENTS.md` was untracked.       | Resolved | Task 1 replaces `AGENTS.md` as the canonical source and reduces `CLAUDE.md` to a pointer.                                                                                                                                                                                                                                                         |
| PRD               | PRD lived outside the repo at `/Users/hasansheikh/Downloads/Factor Lab.md`. | Resolved | Task 1 imports the PRD into `docs/prd.md`; expected heading `FactorLab — Product Requirements Document (PRD)` is present in the source document.                                                                                                                                                                                                  |
| Generated files   | `services/engine/factorlab_engine.egg-info/` was tracked generated output.  | Resolved | Task 2 ignores generated package metadata and removes it from the git index without deleting local artifacts.                                                                                                                                                                                                                                     |
| File size         | Multiple hand-written source files exceeded the 500-line hard maximum.      | Resolved | Tasks 3-5 and Task 10 split production source files across query/action/UI/engine/support modules. `npm run check:file-length` now reports 286 source files at or below the hard limit with no >400-line warnings. Tests, generated Supabase types, and vendored shadcn/ui scaffolding remain excluded from the automated production-source gate. |
| File-length guard | No automated source line-count guard existed.                               | Resolved | Task 2 adds `npm run check:file-length`; Task 10 updates it to check tracked and newly-created source files while excluding docs, tests, generated Supabase types, and vendored shadcn/ui scaffolding.                                                                                                                                            |

## Performance findings

| Area                 | Finding                                                                       | Status         | Evidence / next action                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------- | ----------------------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Query fan-out        | Dashboard and run list pages fetched runs and count through separate queries. | Improved       | Task 6 adds `getRunsList`, a single authenticated bounded query using Supabase `count: "exact"`, and updates `/dashboard` and `/runs` to use it.                                                                                                                                                                                                                                                                                                         |
| Route baseline       | Unauthenticated local route checks redirect to sign-in.                       | Documented     | With `npm run dev:web`, Node fetch returned `/login` 200 in 615ms after fixing the server-action facade, and protected routes redirected to `/login`: `/dashboard` 17.7ms, `/runs` 8.1ms, `/runs/new` 7.2ms, `/data` 7.4ms, `/compare` 5.9ms, `/settings` 4.6ms. Authenticated query counts were not collected because these redirects stop before dashboard page data loaders execute; redirect-path route query count is therefore 0 app page queries. |
| Run creation latency | Smoke test would target a non-local Supabase URL from `.env.local`.           | Blocked safely | `scripts/smoke-test.mjs` inserts runs using the service role. It was not run against non-local Supabase to avoid mutating remote data; rerun only with local Supabase env or explicit remote approval.                                                                                                                                                                                                                                                   |
| Index coverage       | Several workflow scans lacked exact composite indexes.                        | Improved       | Existing schema/migrations cover `runs(user_id, created_at DESC)`, `jobs(run_id)`, `equity_curve(run_id, date)`, `positions(run_id, date)`, `reports(run_id)`, and `prices(ticker, date)`. Task 6 adds `jobs(status, updated_at)` and `data_ingest_jobs(status, updated_at)`.                                                                                                                                                                            |

## Scalability findings

| Area               | Finding                                                                      | Status   | Evidence / next action                                                                                                                                                                                                                      |
| ------------------ | ---------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Future additions   | Passing tests alone does not prove a module will scale with more strategies. | Reviewed | Task 10 ran strict scalability searches for file growth, broad selects/magic limits, hard-coded strategy spread, and schema ownership/index fields. Findings were either fixed or recorded in the strict audit table below.                 |
| Query scalability  | Dashboard and run list routes need bounded, user-scoped reads.               | Improved | `getRunsList` clamps limits to 1-100 and filters by authenticated `user.id`; Task 10 also replaces broad `select("*")` reads in report/job/settings/run/detail/data-health paths with explicit projections and names the ML prediction cap. |
| Schema scalability | Worker/job scans need explicit composite indexes for common state checks.    | Improved | Task 6 migration adds missing composite indexes for `jobs(status, updated_at)` and `data_ingest_jobs(status, updated_at)` using `CREATE INDEX IF NOT EXISTS`.                                                                               |

## Migration and environment findings

| Area              | Finding                                                                                    | Status                   | Evidence / next action                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------- | ------------------------------------------------------------------------------------------ | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Docker            | Initial audit found Supabase blocked because Docker was unavailable.                       | Resolved                 | Docker Desktop was restarted during final verification and `docker version` reports server version `29.5.3`.                                                                                                                                                                                                                                                                              |
| Local Supabase    | Initial audit could not run local Supabase setup.                                          | Resolved with caveat     | Docker is running and `supabase start` reports the local development setup running with API/DB/Studio/Mailpit available. `supabase migration list --local` shows local and remote history aligned through `20260618090000`; optional local services `imgproxy`, `edge_runtime`, and `pooler` remain stopped. Earlier `supabase db reset` applied all migrations through `20260618090000`. |
| Migration order   | Earlier migration filenames lacked full timestamp ordering.                                | Resolved locally         | Task 7 adds `20260220_initial_schema.sql` as a byte-identical migration copy of `supabase/schema.sql` and renames historical date-only migrations to full timestamp filenames with byte-identical contents, so local reset applies migrations in deterministic order.                                                                                                                     |
| Env example       | `.env.example` did not list every runtime key referenced by web, scripts, and worker code. | Resolved                 | Task 7 adds missing diagnostics, Supabase timeout/retry, worker timeout, universe/benchmark, and ML tuning defaults. Key-name inventory printed names only for `.env.local` and `.env.example`; no values were recorded.                                                                                                                                                                  |
| Local env         | Local environment needs core Supabase, worker trigger, cron, and site URL keys.            | Resolved for local setup | Context update confirms `.env.local` has core keys plus generated local `CRON_SECRET`, `WORKER_TRIGGER_SECRET`, and `NEXT_PUBLIC_SITE_URL`; values are not printed or committed.                                                                                                                                                                                                          |
| Remote migrations | Remote Supabase migration target and history must be reconciled before applying changes.   | Blocked safely           | `supabase projects list` works and shows available projects, but `supabase migration list --linked` reports no linked project. No `supabase db push` was run. Existing remotes need target confirmation, backup/restore readiness, comparison of old date-only versions against the new timestamped filenames, and migration-ledger repair before any push.                               |

## Security findings

| Area                  | Finding                                                           | Status   | Evidence / next action                                                                                          |
| --------------------- | ----------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| Secret handling       | Agents need explicit rules to avoid leaking env and service keys. | Resolved | Task 1 adds environment safety rules to `AGENTS.md`; audit records key presence only, never values.             |
| RLS and isolation     | RLS, guest isolation, and report ownership need a focused review. | Reviewed | Task 8 verifies RLS policy tests, auth callback tests, and ingest-job route ownership regression coverage.      |
| Service-role boundary | Service-role usage must stay server-only and narrowly scoped.     | Improved | Task 8 fixes service-role ownership gaps in benchmark ingest jobs and worker trigger auth.                      |
| Cron and worker auth  | Cron and worker trigger secrets must be required and fail closed. | Improved | Cron and worker trigger tests prove privileged work is gated behind configured bearer secrets and fails closed. |
| Rate limits           | Publicly reachable mutation endpoints need abuse boundaries.      | Reviewed | Manual benchmark ingest uses Upstash sliding-window limiting when configured; production must configure it.     |
| Env exposure          | Environment audits must not expose secret values.                 | Reviewed | Task 7/8 docs record key presence and behavior only; `.env.local` remains ignored and uncommitted.              |

| Finding                                                                                                                                                                                                                                                       | Evidence                                                                                                                                                                                                                     | Fix                                                                                                                                                                                                             | Status                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authenticated users could poll or cancel `data_ingest_jobs` owned by other users because `/api/data/ingest-benchmark` used the service-role client without an ownership check on `GET`, single-job `DELETE`, `cancelAll`, and active-job reuse during `POST`. | Failing regression tests in `app/api/data/ingest-benchmark/route.test.ts` showed cross-user `GET`/`DELETE` returning 200, bulk cancel lacking a `requested_by_user_id` filter, and `POST` reusing another user's active job. | Added ownership checks for `requested_by_user_id` and linked `requested_by_run_id` ownership, scoped `cancelAll` to the authenticated user's manual jobs, and skipped other users' active jobs during creation. | Resolved; `npm run test:run -- app/api/data/ingest-benchmark/route.test.ts app/api/cron/_lib/refresh.test.ts app/auth/callback/route.test.ts app/actions/auth.test.ts app/actions/reports.test.ts app/actions/runs.test.ts lib/supabase/isolation-rls.test.ts` passes 66 tests. |
| Cron refresh endpoints must fail closed before touching the service-role client.                                                                                                                                                                              | `app/api/cron/_lib/refresh.test.ts` covers missing, wrong, guest-session, and user-session requests without the bearer secret.                                                                                               | Existing `assertAuthorized` requires `CRON_SECRET` and exact `Authorization: Bearer <secret>` before `createAdminClient()`.                                                                                     | Resolved by existing code and tests.                                                                                                                                                                                                                                            |
| Worker trigger HTTP server accepted `POST /trigger` without authorization when `WORKER_TRIGGER_SECRET` was unset.                                                                                                                                             | A failing regression in `services/engine/tests/test_worker_http_server.py` showed an unset secret returned 200 and woke the worker.                                                                                          | The trigger handler now returns 503 when the secret is missing, returns 401 for a wrong bearer token, and wakes the worker only for the configured bearer token.                                                | Resolved; `cd services/engine && /tmp/factorlab-pytest-venv/bin/python -m pytest tests/test_worker_http_server.py` passes 3 tests.                                                                                                                                              |
| Report generation and run deletion use service-role storage/database writes.                                                                                                                                                                                  | `app/actions/reports.ts` reads the caller through the server client and relies on RLS/user session before admin upload/upsert; `app/actions/runs/delete-run.ts` checks `run.user_id === user.id` before admin cleanup.       | No code change needed; existing tests in `app/actions/reports.test.ts` and `app/actions/runs.test.ts` cover ownership/error paths.                                                                              | Reviewed; keep covered in future changes.                                                                                                                                                                                                                                       |
| Guest authentication and account upgrade flows must not bypass isolation.                                                                                                                                                                                     | `app/auth/callback/route.test.ts`, `app/actions/auth.test.ts`, and `lib/supabase/isolation-rls.test.ts` cover guest redirects, guest upgrade paths, and Supabase user-scoped access assumptions.                             | No code change needed in Task 8.                                                                                                                                                                                | Reviewed; keep covered in future changes.                                                                                                                                                                                                                                       |
| Manual benchmark ingest rate limiting depends on Upstash configuration.                                                                                                                                                                                       | `checkIngestRateLimit` applies a 5 ingests/hour sliding window when `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` are present, and allows requests when those keys are missing or the rate-limit call fails.       | No code change in Task 8; `.env.example` and deployment docs now mark the keys as recommended production configuration.                                                                                         | Reviewed; production deployments should configure Upstash before public traffic, and this should become fail-closed before adding unauthenticated or higher-volume ingest endpoints.                                                                                            |

## Strict scalability audit

| Check                            | Result   | Evidence                                                                                                                                                                                                                                                                                                            | Follow-up                                                                                                                                                |
| -------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| File growth guard                | Pass     | `npm run check:file-length` reports 286 production source files at or below 500 lines with no >400-line warnings. The largest remaining files over 400 lines are excluded tests, generated Supabase types, or vendored shadcn/ui scaffolding.                                                                       | Keep `npm run check:file-length` in the full validation path before adding behavior.                                                                     |
| Broad query and magic-limit scan | Pass     | Strict ripgrep scans for broad wildcard selects, 500/1000-row magic limits, TODOs, and FIXMEs return no matches after replacing broad row reads with explicit projections and naming the ML prediction cap.                                                                                                         | Continue requiring explicit projections for dashboard, report, job, settings, compare, and data-health reads.                                            |
| Strategy extensibility           | Reviewed | Strategy ids remain intentionally centralized in UI labels/schema/warmup/coverage helpers and in engine dispatch modules. The broad search shows dispatch concentration in `services/engine/factorlab_engine/worker/execution.py`, `worker/settings.py`, `ml_training_models.py`, and shared TS strategy constants. | Adding a seventh strategy should start by extending the shared TS strategy schema/labels, warmup/coverage constants, and engine dispatch tests together. |
| Schema and ownership indexes     | Pass     | Migration/schema scan confirms `runs.user_id`, child-table `run_id` references, RLS ownership policies, `runs(user_id, created_at DESC)`, `jobs(status, updated_at)`, and `data_ingest_jobs(status, updated_at)` indexes are present.                                                                               | Before remote migration push, confirm the target project and backup plan because this branch intentionally avoids remote `supabase db push`.             |
| Runtime route scalability        | Pass     | Local `npm run dev:web` route probe returns `/login` 200 in 615ms and protected route redirects in 4.6-17.7ms without server compile errors after replacing the `app/actions/auth.ts` server-action barrel with async wrappers.                                                                                     | Authenticated dashboard timing still needs a signed-in browser session or local seeded auth user.                                                        |

## Final verification

| Command / check                                                     | Result                | Notes                                                                                                                                                                      |
| ------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run format:check`                                              | Pass                  | Prettier reports all matched files use configured style.                                                                                                                   |
| `npm run lint`                                                      | Pass                  | ESLint completes with `--max-warnings=0`.                                                                                                                                  |
| `npm run typecheck`                                                 | Pass                  | `tsc --noEmit` completes successfully.                                                                                                                                     |
| `npm run test:run`                                                  | Pass                  | 55 Vitest files / 444 tests pass. Test stderr includes expected chart zero-size warnings and storage invalid-bucket warnings.                                              |
| `npm run check:file-length`                                         | Pass                  | 286 production source files checked; no hard-limit failures or target warnings.                                                                                            |
| `cd services/engine && ruff format . && ruff check .`               | Pass                  | Ruff reports 47 files unchanged and all checks passed.                                                                                                                     |
| `cd services/engine && ../../.tmp-engine-venv/bin/python -m pytest` | Pass                  | 210 Python tests pass in 89.57s.                                                                                                                                           |
| Runtime route probe with `npm run dev:web`                          | Pass with auth caveat | `/login` renders 200; protected routes redirect to `/login` quickly while unauthenticated.                                                                                 |
| `scripts/smoke-test.mjs`                                            | Blocked safely        | Current `.env.local` Supabase URL is not local, and the script uses the service role to insert runs. It was not run against a non-local project without explicit approval. |
| `supabase migration list --local`                                   | Pass with ops caveat  | Local and remote migration history align through `20260618090000`; optional local `imgproxy`, `edge_runtime`, and `pooler` services are stopped.                           |
