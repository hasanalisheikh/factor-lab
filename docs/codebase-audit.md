# FactorLab Codebase Audit

## Status

This ledger tracks stabilization findings and their disposition. Secret values are intentionally not
recorded here; environment checks should confirm key presence only.

## Structural findings

| Area              | Finding                                                                     | Status      | Evidence / next action                                                                                                                                                                                                                                |
| ----------------- | --------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Agent policy      | `CLAUDE.md` duplicated shared policy while `AGENTS.md` was untracked.       | Resolved    | Task 1 replaces `AGENTS.md` as the canonical source and reduces `CLAUDE.md` to a pointer.                                                                                                                                                             |
| PRD               | PRD lived outside the repo at `/Users/hasansheikh/Downloads/Factor Lab.md`. | Resolved    | Task 1 imports the PRD into `docs/prd.md`; expected heading `FactorLab — Product Requirements Document (PRD)` is present in the source document.                                                                                                      |
| Generated files   | `services/engine/factorlab_engine.egg-info/` was tracked generated output.  | Resolved    | Task 2 ignores generated package metadata and removes it from the git index without deleting local artifacts.                                                                                                                                         |
| File size         | Multiple hand-written source files exceed the 500-line hard maximum.        | In progress | Tasks 3-5 split the largest query/action/UI/engine files. Remaining known oversized files include `lib/coverage-check.ts`, dashboard/data/supporting components, and several tests; final `npm run check:file-length` must prove the backlog is gone. |
| File-length guard | No automated source line-count guard existed.                               | Resolved    | Task 2 adds `npm run check:file-length`. It is expected to fail until all oversized source files are split.                                                                                                                                           |

## Performance findings

| Area                 | Finding                                                                       | Status         | Evidence / next action                                                                                                                                                                                                                                                                                                                                                               |
| -------------------- | ----------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Query fan-out        | Dashboard and run list pages fetched runs and count through separate queries. | Improved       | Task 6 adds `getRunsList`, a single authenticated bounded query using Supabase `count: "exact"`, and updates `/dashboard` and `/runs` to use it.                                                                                                                                                                                                                                     |
| Route baseline       | Unauthenticated local route checks redirect to sign-in.                       | Documented     | With `SKIP_FACTORLAB_WORKER=1 npm run dev:web`, curl returned 307 redirects: `/dashboard` 0.052s, `/runs` 0.031s, `/runs/new` 0.018s, `/data` 0.013s, `/compare` 0.011s, `/settings` 0.014s. Authenticated query counts were not collected because these redirects stop before dashboard page data loaders execute; redirect-path route query count is therefore 0 app page queries. |
| Run creation latency | Smoke test would target a non-local Supabase URL from `.env.local`.           | Blocked safely | `scripts/smoke-test.mjs` inserts runs using the service role. It was not run against non-local Supabase to avoid mutating remote data; rerun only with local Supabase env or explicit remote approval.                                                                                                                                                                               |
| Index coverage       | Several workflow scans lacked exact composite indexes.                        | Improved       | Existing schema/migrations cover `runs(user_id, created_at DESC)`, `jobs(run_id)`, `equity_curve(run_id, date)`, `positions(run_id, date)`, `reports(run_id)`, and `prices(ticker, date)`. Task 6 adds `jobs(status, updated_at)` and `data_ingest_jobs(status, updated_at)`.                                                                                                        |

## Scalability findings

| Area               | Finding                                                                      | Status   | Evidence / next action                                                                                                                                                                                                                                              |
| ------------------ | ---------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Future additions   | Passing tests alone does not prove a module will scale with more strategies. | Planned  | Task 10 must perform a strict scalability audit before completion: every remaining large file, broad shared helper, unbounded query, missing ownership filter, and hard-coded strategy/worker branch must be either resolved or recorded with a concrete follow-up. |
| Query scalability  | Dashboard and run list routes need bounded, user-scoped reads.               | Improved | `getRunsList` clamps limits to 1-100 and filters by authenticated `user.id`; targeted test verifies a single `runs` query, user filter, search/status filters, and bounded limit.                                                                                   |
| Schema scalability | Worker/job scans need explicit composite indexes for common state checks.    | Improved | Task 6 migration adds missing composite indexes for `jobs(status, updated_at)` and `data_ingest_jobs(status, updated_at)` using `CREATE INDEX IF NOT EXISTS`.                                                                                                       |

## Migration and environment findings

| Area              | Finding                                                                                    | Status                   | Evidence / next action                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------- | ------------------------------------------------------------------------------------------ | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Docker            | Initial audit found Supabase blocked because Docker was unavailable.                       | Resolved                 | Context update confirms Docker Desktop is installed and running; local check reports Docker server version `29.5.3`.                                                                                                                                                                                                                                                                      |
| Local Supabase    | Initial audit could not run local Supabase setup.                                          | Resolved with caveat     | Docker is running and `supabase start` succeeded after stopping the older local `factor-lab` stack that held port `54322`. `supabase db reset` applies all migrations and records latest version `20260618090000`, but exits nonzero during post-reset container restart because Kong keeps a stale storage upstream IP; restarting only the local Kong container clears the storage 502. |
| Migration order   | Earlier migration filenames lacked full timestamp ordering.                                | Resolved locally         | Task 7 adds `20260220_initial_schema.sql` as a byte-identical migration copy of `supabase/schema.sql` and renames historical date-only migrations to full timestamp filenames with byte-identical contents, so local reset applies migrations in deterministic order.                                                                                                                     |
| Env example       | `.env.example` did not list every runtime key referenced by web, scripts, and worker code. | Resolved                 | Task 7 adds missing diagnostics, Supabase timeout/retry, worker timeout, universe/benchmark, and ML tuning defaults. Key-name inventory printed names only for `.env.local` and `.env.example`; no values were recorded.                                                                                                                                                                  |
| Local env         | Local environment needs core Supabase, worker trigger, cron, and site URL keys.            | Resolved for local setup | Context update confirms `.env.local` has core keys plus generated local `CRON_SECRET`, `WORKER_TRIGGER_SECRET`, and `NEXT_PUBLIC_SITE_URL`; values are not printed or committed.                                                                                                                                                                                                          |
| Remote migrations | Remote Supabase migration target and history must be reconciled before applying changes.   | Blocked safely           | `supabase projects list` works and shows available projects, but `supabase migration list --linked` reports no linked project. No `supabase db push` was run. Existing remotes need target confirmation, backup/restore readiness, comparison of old date-only versions against the new timestamped filenames, and migration-ledger repair before any push.                               |

## Security findings

| Area                  | Finding                                                            | Status      | Evidence / next action                                                                              |
| --------------------- | ------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------- |
| Secret handling       | Agents need explicit rules to avoid leaking env and service keys.  | Resolved    | Task 1 adds environment safety rules to `AGENTS.md`; audit records key presence only, never values. |
| RLS and isolation     | RLS, guest isolation, and report ownership need a focused review.  | Planned     | Audit migrations and Supabase access paths after setup stabilization.                               |
| Service-role boundary | Service-role usage must stay server-only and narrowly scoped.      | Planned     | Review Next.js actions, route handlers, and worker code in later security tasks.                    |
| Cron and worker auth  | Cron and worker trigger secrets must be required and rate-limited. | In progress | Local secrets are reported generated by context; code-level enforcement still needs review.         |
